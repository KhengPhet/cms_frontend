# On Railway, set VITE_API_URL as an environment variable
# in the frontend service settings BEFORE building.
# Example: VITE_API_URL=https://your-backend.up.railway.app/api
#
# This file is the fallback for local production builds only.
VITE_API_URL=http://localhost:5001/api
